import React, { FC } from 'react'
import { KeyCodes, useKeyboard } from 'src/use-keyboard'

const IndexPage: FC = () => {
    return (
        <div>
            <h1>Backlog</h1>
        </div>
    )
}

export default IndexPage;